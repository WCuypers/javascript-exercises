const findTheOldest = function(peopleArray) {
    const currentYear = 2022;
    let age;
    for (i = 0 ; i < peopleArray.length; i++) {    // for each element of the array
        age = yearOfDeath - yearOfBirth;
        return age
            // store result in age variable    
    }
    // return name linked to highest age variable
};

// Do not edit below this line
module.exports = findTheOldest;
